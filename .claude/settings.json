{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -qE '(flake\\.lock|\\.zcompdump)'; then echo 'BLOCK: Do not edit generated files' >&2; exit 2; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATH\" | grep -q 'flake.nix'; then cd ~/dotfiles/nix/nix && nix flake check 2>&1 | head -20; fi"
          }
        ]
      }
    ]
  }
}
