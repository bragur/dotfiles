.DS_Store
karabiner/.config/karabiner/automatic_backups/
lazyvim.bragur
tmux/.config/tmux/*.log
ghostty/.config/ghostty/local.config
result
result-*
.bottega/

# Machine-local shell config (protective: if stow ever folds these dirs,
# the local files would land physically inside the repo)
zsh/.config/zsh/local.zsh
zsh-abbr/.config/zsh-abbr/local-abbreviations
